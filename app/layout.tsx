import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import "./globals.css"

export const metadata: Metadata = {
  title: "Crux Detailing - Premium Auto Detailing & Ceramic Coating | Vancouver & Lower Mainland",
  description:
    "Professional mobile auto detailing and ceramic coating services across Vancouver, Surrey, Burnaby, Coquitlam, and Richmond. Premium protection with warranty.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>{children}</body>
    </html>
  )
}
